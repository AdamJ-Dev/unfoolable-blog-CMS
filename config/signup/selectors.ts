import signupSpec from './spec.json';

export const getUsernameSignupSpec = () => {
  return signupSpec.username;
};

export const getPasswordSignupSpec = () => {
  return signupSpec.password;
};

export const getPasscodeSignupSpec = () => {
  return signupSpec.passcode;
};
